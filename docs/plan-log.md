# plan.md 갱신 로그

`docs/plan.md` 의 변경 이력. plan.md 가 컨텍스트로 자주 로드되므로 로그는 분리.
새 엔트리는 최신이 아래로 가도록 시간순 append.

| 날짜 | 내용 |
|---|---|
| 2026-05-13 | 초기 작성. Phase 0~5 정의, 사용자 시나리오 + gap analysis 포함 |
| 2026-05-13 | 최종 검토 반영: Phase 1.5(서명검증), 2.6/2.7(에러알림·truncate), 4.0(mock→real), 4.6(redaction), 5.5~5.7(정리) 추가. Agent 활용 지점 명시 |
| 2026-05-13 | Phase 1 완료 (5 commits, 104 tests). Phase 1.6 추가 — DB 레이어 SQLAlchemy/Alembic, MySQL dev/prod + in-memory SQLite test, 테스트 패키지별 격리 |
| 2026-05-13 | Phase 1.6 완료 (90 tests). 테스트 패키지별 이동, SQLAlchemy 2.0 + Alembic, async store, docker-compose MySQL 8.0 |
| 2026-05-13 | Phase 1.7 정식화 + 완료 (90 tests / 0 warnings). init_schema SQLite-only, .claude/rules 5개 분리, common.clock + ruff DTZ + StrEnum, ruff lint+format 전면, pre-commit. 5.6 (datetime cleanup) 은 1.7.3 에 흡수돼 제거. 5.7 GitHub Actions CI 신규 |
| 2026-05-13 | Phase 0 (0.1 Slack / 0.2 Sentry / 0.2b ngrok / 0.3 GitHub App) 완료. Sentry 실 webhook E2E 검증 중 `Sentry-Hook-Signature` 헤더 버그 발견·수정 (`ecb1182`) |
| 2026-05-14 | Phase 2 (Slack 가시성) 완료 — 6 commits (`c2feda2`~`0b6fb39`) + fix (`4000cfa`). 실 LLM + mock 양쪽 E2E 캡처 확보. 발견 잔무: Slack section truncation (2.8, patch=3044 / RCA=4146 실측), 실 LLM agent 단위 thread emit 누락 (2.9). 우선순위 재조정: 다음 주 Phase 4 (실 코드 변경 PR — unified diff prompt + git apply) 가 Phase 3 보다 1순위 |
| 2026-05-14 | 갱신 로그를 plan.md → plan-log.md 로 분리 (plan.md 컨텍스트 비용 절감) |
| 2026-05-14 | gateway 패키지 layered 정리 — api/services/infrastructure 3-layer. `IncidentStore`→`IncidentRepository`. `parsers`→`monitors`. `.claude/rules/architecture.md` 신설 (import 방향 + naming 룰). Phase 6 신설 (logger / orchestrator 실 API tool) |
| 2026-05-18 | Phase 4 (실 코드 변경 PR) 완료 → PR #1 rebase merge (9 commits). `common/diff.py` (extract/changed_paths/is_new_file/verify_apply/apply_diff) + `common/redact.py` (9 secret patterns) + `github.app:_apply_diff_pr` (Git Data API 10-call) + `github.app:close_pr` + `gateway.services.decisions:_close_pr_if_exists`. `incidents` 테이블에 `pr_number/pr_branch` 컬럼 (Alembic `f858338542cd`). 144 tests, e2e 실 PR 검증 (`dlwodnjs724/warroom-demo`) |
| 2026-05-18 | Phase 4 머지 직전 cold-context sub-agent 2-round 리뷰 — 1차 코드 (5 findings, HIGH 3건 fix: redaction-vs-diff 충돌·sk- 패턴 너무 광범·PR persist 실패 시 orphan), 2차 architecture (4 findings, HIGH 1건 `GitHubAppClient` 책임 비대화는 follow-up 으로 분리). 검증된 패턴 → Phase 3 머지 전에도 동일 적용 권장 |
| 2026-05-18 | Architecture follow-up 이슈 4건 신설: [#2](https://github.com/dlwodnjs724/warroom/issues/2) `GitHubAppClient` 분해, [#3](https://github.com/dlwodnjs724/warroom/issues/3) gateway GitHub client DI, [#4](https://github.com/dlwodnjs724/warroom/issues/4) `common/diff.py` 분리, [#5](https://github.com/dlwodnjs724/warroom/issues/5) async to_thread + close_pr observability. Phase 3 / 6.2 진입 전 #2~#4 정리 권장 (새 transport 추가 시점 임박) |
| 2026-05-18 | plan.md / plan-log.md / architecture.md / decisions.md stale 갱신 — Phase 4 완료 반영 (현재 가능 매트릭스, 의존 그래프, 추천 순서, agent 활용 패턴) |
| 2026-05-18 | Architecture follow-up bundle 머지 → PR #6 (6 commits, 4 refactor + 2 review fix). 단일 worktree sub-agent 가 #2/#3/#4 한꺼번에 (충돌 매트릭스로 분리 worktree 비효율). cold-context 리뷰 6 findings → HIGH 1 (gateway pyproject `warroom-github` 누락) + MEDIUM 2 (`dry_run` dict 필드 → `is_dry_run: bool` Protocol 속성) 즉시 반영. MEDIUM 3 (orphan branch on commit_files + open_pr 비원자성) → 별도 #7 로 분리. 149 tests pass |
| 2026-05-18 | Sub-agent 워크플로 검증 패턴: bundle 단일 worktree agent → PR open → cold-context 리뷰 → fix push → rebase merge. Phase 4 의 "self-review + cold-context" 패턴이 architecture refactor 에도 그대로 작동. 충돌 매트릭스 진입 전 검사 시 분리 worktree 가 의미 없는 경우 명확히 식별 가능 |
| 2026-05-18 | PR reliability bundle 머지 → PR #8 (4 commits: 3 refactor + 1 review fix). 단일 worktree agent 가 #5 + #7 한꺼번에. cold-context 리뷰 4 findings 모두 반영 — MEDIUM 1 (transport 분류가 reject 만 대상, approve raw httpx 로 500) 이 핵심 push back: `_check(resp, context)` helper 신설 → app.py 의 모든 raise_for_status 가 `_classify_status` 경유. LOW 2 (429 transient) / LOW 3 (orphan 로그 paste-ready) / LOW 4 (TestClient 가 이미 worker thread 인 점 우회 → asyncio.to_thread 자체 monkeypatch) 모두 반영. 170 tests pass |
| 2026-05-18 | Phase 4 후속 architectural follow-up 5건 전부 closed (#2/#3/#4/#5/#7). 코드 빚 청소 완료 — 다음 1순위는 Phase 3 (Slack Interactivity), 2순위 Phase 4.0/6.2 (실 Sentry/GitHub tool), 3순위 Phase 6.1 (logging). `[WARROOM][cleanup]` / `[WARROOM][open_pr]` / `[pr_builder][ORPHAN]` 같은 prefix 패턴이 누적되어 logger 도입 시점이 명확히 ROI 큰 시점으로 도달 |
| 2026-05-18 | Convention codify + clients/ 그룹핑 머지 (PR #9, 3 commits). github/chatops 의 client 구현을 `clients/` 디렉터리로 분리 (Protocol contract ↔ implementations visual boundary). `.claude/rules/layering.md` §4a/§4b/§4c 신규 절: 패키지 내부 layer (contract / clients / usecase / infra helper) + external adapter error 계층 (`<Tool>Error` → `AuthError` / `TransientError`) + composition root 패턴 (services 가 env / factory 직접 호출 금지). `.claude/rules/agents.md` 신설 — 4회 검증된 sub-agent / cold-context review 패턴 codify (충돌 매트릭스 → bundle 결정, worktree isolation, cold review trigger, rebase merge, GitHub `closes #N #M` auto-close 한계). 170 tests pass / ruff clean |
| 2026-05-18 | Phase 3 (Slack Interactivity) 구현 + PR open. 6 commits on `feat/phase-3-slack-interactivity`. (1) `verify_slack_signature` — v0 HMAC-SHA256(`v0:{ts}:{body}`, signing_secret) + 5분 replay 윈도우, secret 미설정 시 dev skip. (2) `incidents.rejection_reason` Text 컬럼 + Alembic `6d3aecc468a3` (report 가 NULL 일 수 있는 분석 전 반려 케이스도 사유 보관 위해 incident 단위). (3) `handle_decision(rejection_reason=None)` + HTTP `/reject` optional body. (4) `SlackNotifier.open_reject_modal` (callback_id `warroom_reject_modal`, private_metadata 에 incident_id, plain_text_input multiline). (5) `gateway/api/slack.py` — block_actions (approve → BackgroundTasks, reject → `asyncio.to_thread(open_reject_modal)` since trigger_id 3초 TTL) + view_submission (reason 추출 → BackgroundTasks reject) + 서명 검증 (raw body). composition root 에 `get_slack_notifier()` singleton 추가. 170 → 198 tests pass (+28). plan §0 sticky / §4 Phase 3 / §3 매트릭스 / architecture.md sequence / decisions.md 갱신. **재분석 hook (Phase 3.5 의 컨텍스트 주입 부분) 은 의도적으로 별도 follow-up 으로 분리** — 현재 시스템에 재분석 자체가 없어 hook 만 박으면 dead code. PR open → cold-context review 대기 |
| 2026-05-20 | Phase 3 머지 (PR #10, 10 commits rebase merge). 1차 cold review HIGH 3건 + M2 1건 즉시 fix: H1 `time.time()` → `common.clock.now().timestamp()` (datetime.md 룰 위반), H2 lifespan eager init SlackNotifier (env credential snapshot timing), H6 `rejection_reason` redaction + 4000자 캡 (secrets.md § 3 위반 + DoS 방지), M2 incident_id 빈 값 가드 대칭화. 사용자 audit 지적으로 (a) `api/slack.py` 의 외부 어댑터 직접 호출 + dispatch 로직을 `services/slack_interactions.py` 로 분리 (§ 2 위반 fix, webhooks.py 패턴 일치), (b) `RejectBody` DTO 를 `api/incidents_schemas.py` 로 분리. **`.claude/rules/layering.md` § 7 신설** — HTTP request/response DTO 는 `api/<domain>_schemas.py` 페어 (도메인 모델 `common/models.py` 와 구분). 2차 cold review: No HIGH, M2 (이미 fix 됨) + LOW 만, merge 권장. 204 tests pass (170 → 204, +34). Follow-up 이슈 3건 (#11 signature helpers / #12 InteractiveNotifier Protocol / #13 pipeline make_notifier). **사용자 audit pattern 가치 재확인**: cold review 가 못 잡는 도메인 레벨 책임 분배 (api vs services 의 dispatch 책임) 를 사용자 한 마디로 발견 — multi-agent 환경에서 cold review + 사용자 audit 2-stage 패턴이 안정적 |
| 2026-05-20 | Phase 3 follow-up #11/#12/#13 bundle worktree 처리. 3 atomic refactor commits (의존 적은 순: #13 → #12 → #11) + docs commit. (1) **#13** `services/pipeline.py` 의 `from chatops.clients.factory import make_notifier` 직접 호출 제거 → `dependencies.make_pipeline_notifier(persist_cb, lookup_cb)` wrapper 신설 (캐싱 없음 — 매 incident 마다 fresh notifier, thread 영속화 상태 독립). 순환 import 회피 패턴: pipeline 이 dependencies 의 wrapper 를 호출하면서 자기 콜백을 인자로 전달, dependencies 는 pipeline 모름. (2) **#12** `chatops/base.py` 에 `InteractiveNotifier` Protocol 추가 (open_reject_modal 만 contract), `Notifier` ABC 는 무변경. `get_slack_notifier` 반환 타입을 InteractiveNotifier 로 변경 — 실 SlackNotifier 가 structural typing 으로 자동 매칭, consumer 가 필요한 표면만 의존. (3) **#11** `infrastructure/monitors/security.py` → `services/security.py` rename + secret 을 함수 인자로 받게 시그니처 변경 (verify_sentry_signature(body, sig, secret) / verify_datadog_token(token, expected) / verify_slack_signature(body, sig, ts, secret)). secret None/빈 문자열 → dev-skip 동작 유지. `dependencies.py` 에 3개 getter 추가 (`get_sentry_secret` / `get_datadog_token` / `get_slack_signing_secret`) — `get_github_repo` 와 동일 패턴, 캐싱 없음. `warn_if_secrets_missing` 도 인자 3개로 변경 + composition root 가 wiring. 테스트는 `monkeypatch.setenv` 대신 직접 인자로 검증 (더 깔끔). `_now` patch 경로 동기화 (test_security.py + test_slack_interactions.py). 204 tests pass 유지. layering.md § 2 (api → infrastructure 직접 호출 제거: webhook 서명 검증은 services 책임) + § 4c (services 가 env / factory 직접 호출 금지) 완전 준수 |
| 2026-05-20 | PR #14 머지 (5 commits rebase, 14 files +188/-103). Cold review (cold-context sub-agent): HIGH/MEDIUM 0건, LOW 4건 모두 skip (skip 이유 PR comment 명시 — agents.md § 3 룰). 사용자 audit 결과 1건 반영 (audit fix commit `e1f7d68`): `dependencies.py` docstring 이 GitHubClient 만 언급해 #11/#12/#13 이후 책임 (env getter 3개, slack notifier, pipeline notifier wrapper) 가 stale → composition root 책임 전체 + 캐싱 정책 3분류 (lazy singleton / 매 호출 env read / factory wrapper) 로 갱신. **사용자 audit 가치 3회째 확인**: cold reviewer 가 못 잡은 "PR 로 모듈 책임 범위가 넓어졌는데 docstring 만 stale" 종류의 audit — 룰북에 박을 패턴은 아니지만 audit pattern 자체가 매번 가치 발견. follow-up #11/#12/#13 모두 auto-close (PR body `Closes #11\nCloses #12\nCloses #13` 줄바꿈 패턴) |
| 2026-05-24 | Phase 5 잔무 5.5 / 5.6 / 5.7 처리. **5.5** (stuck `ANALYZING` → `FAILED` 복구) 는 이미 `IncidentRepository.recover_stale_analyzing()` + `gateway/main.py` lifespan wiring + `TestRecoverStaleAnalyzing` 6 케이스로 완료된 상태 — plan stale 정리만. **5.6** e2e 통합 테스트 1건 신설 (`tests/test_e2e_pipeline.py:test_webhook_to_approve_full_path`) — 기존 test_gateway 는 `run_incident_pipeline` 자체를 patch 해 webhook 응답만 검증했고 파이프라인 실제 실행 + 전이 검증이 누락. LLM 봉인은 `monkeypatch.setattr(runner_mod, "_USE_MOCK", True)` (module-level attribute 라 `MOCK_PIPELINE` env 만으론 부족) + `time.sleep` no-op (mock 의 1초 × 4 → 0초). webhook → AWAITING_APPROVAL + report 영속화 → approve → PR dry-run → APPROVED 한 호흡. **5.7** GitHub Actions CI (`.github/workflows/ci.yml`) — `astral-sh/setup-uv@v3` 캐시 + `uv sync --all-extras --dev` → `ruff format --check` / `ruff check` / `pytest -v`. push/PR to main 트리거 + concurrency cancel-in-progress + 10분 timeout. CI 도입 전 baseline format 정리 commit 별도 (`test_redact.py` / `slack_interactions.py` — implicit string concat 합치기, ruff 0.7.0 표준). 211 tests pass (+1) |
| 2026-05-24 | plan stale 4 commit audit. 발표 전후 (2026-05-21) push-only 핫픽스 4건이 sticky / Phase 매핑에 미반영된 상태였음: (`f12cae6` Phase 5.5 startup 복구 — 본 audit 직전 5.5 commit 으로 발견됨), (`1f2ad59` Phase 2.9 실 LLM agent thread emit — `_run_crew_pipeline` Crew kickoff 전후 `on_agent_update` 추가), (`cc445de` decision thread reply + actor mention + `run_coroutine_threadsafe` self-wait deadlock 해소 — async endpoint 가 같은 loop 에 future 던지고 sync `.result()` 대기하던 critical bug, `make_pipeline_notifier(lookup_cb=_lookup_slack_thread)` 교체 + 모든 notifier 호출 `await asyncio.to_thread(...)` wrap), (`508dab5` 시연 도구 `scripts/demo_raise.py` + sentry-sdk/matplotlib 의존). sticky "이전 완료" 시간 역순 재정렬 + Phase 2.9 ⚠️ → ✅. **교훈**: 발표·시연 직전 핫픽스는 PR 없이 main 직 push 되기 쉽고 (시간 압박) 이후 plan 갱신이 누락되면 다음 작업 때 "이거 이미 됐네?" 비용 발생. CI 도입했으니 다음부터는 main 직 push 도 일단 검증은 강제. |
| 2026-06-03 | 자율 묶음 4건 머지: **#15** `.github/dependabot.yml` + `pre-commit-autoupdate.yml` (주간 cron), **#14** graceful shutdown — `services/pipeline._in_flight` dict + `drain_in_flight(timeout)` (cancel + status FAILED, 5.5 startup recovery 와 짝), **#3** Phase 2.8 — `_post_full_text_if_truncated` (RCA/patch 한도 초과 시 thread 풀텍스트, redact 통과), **#9** Retry-After header-aware backoff — `_parse_retry_after` (RFC 7231 두 형태) + `GitHubTransientError.retry_after`, decisions 응답 / Slack thread 메시지에 hint. CI 첫 빌드 시 `astral-sh/setup-uv@v8` floating tag 없어 `v8.1.0` 명시. 230 pass. |
| 2026-06-03 | 자율 묶음 6건 머지 (Phase 5 잔무 외): **#6** actions Node.js 24 (checkout@v6 / setup-uv@v8), **#13** `/healthz` (api/system.py — liveness only, DB 의존성 제외), **#11** Warroom self-monitoring `dependencies.init_sentry()` (`SENTRY_DSN`, dogfooding), **#4** architecture.md incident state machine (mermaid + 불변 조건 4건), **#8** plan/decisions stale 정리 (마일스톤 ⬜→✅, 발표 전 핫픽스 반영), **#12** `docs/demo.md` end-to-end walkthrough + 트러블슈팅 매트릭스. 215 → 230 pass. CI 22초 green. |
| 2026-06-03 | **Phase 4.0 / 6.2 머지** (mock → real Sentry/GitHub tool). 사용자가 Sentry Internal Integration 의 Auth Token 발급해 `.env` 에 넣어준 후 GO. (1) `orchestrator/tools/sentry.py` — `SENTRY_AUTH_TOKEN` 설정 시 `GET /issues/{id}/` + `/events/latest/` 호출, title/culprit/count/userCount/stacktrace/release/environment 추출 → LLM 친화 텍스트. 토큰 없으면 mock fallback, 호출 실패 시 broad except → mock. (2) `orchestrator/tools/github.py` — `make_github_client()` 호출 + `is_dry_run` 분기. 실 client 면 `list_commits(repo, path, limit=5)` (Protocol 신규 메서드 — `GET /repos/{repo}/commits?path=&per_page=`) + `get_file_content` (head 80줄). 빈 list / 호출 실패 → mock. (3) `GitHubClient` Protocol 확장 — `GitHubAppClient.list_commits` 구현 + `DryRunGitHubClient.list_commits` (empty). 룰 § 4b 면제 (LLM tool 표면이라 caller 분기할 의미 있는 예외 없음, 모든 비정상 mock 으로 surface). conftest 의 delenv 에 `SENTRY_AUTH_TOKEN` / `SENTRY_ORG` 추가 (secrets.md § 5). `.env.example` 의 TODO 라인 → 실 가이드 (`SENTRY_ORG=warroom`). memory `project_phase_6_deferred.md` 갱신 — 6.2 ✅, 6.1 만 남음. 237 → 248 pass (+11). |
| 2026-06-04 | **Phase 6.1 머지** (print → logging). 사용자 GO 신호로 memory deferred 해제. (1) `common/logging.py` 신설 — `configure_logging()` (idempotent, env 기반 LEVEL/FORMAT 분기) + `JsonFormatter` (record → structured JSON, exc_info / args interpolation 지원). (2) 전 패키지 `print` prefix 패턴 (`[WARROOM][slack]` / `[pr_builder][ORPHAN]` / `[SlackNotifier]` / `[GitHubAppClient]` / `[orchestrator.tools.*]` 등) → `logging.getLogger(__name__)` + level 별 호출. ERROR (orphan branch 운영 alert) / WARNING (transient transport / 분류 실패) / INFO (lifecycle). (3) 영향 범위: gateway/main + services/* (ingest/pipeline/decisions/security/slack_interactions) + chatops/clients/slack.py + github/clients/{app,dry_run}.py + github/pr_builder.py + orchestrator/tools/{sentry,github}.py — 11 파일. (4) **유지** (의도적 print): `chatops/clients/console.py` (ConsoleNotifier 가 본질이 콘솔 UI), `demo.py` (CLI 사용자 인터랙션). (5) demo.py / gateway/main.py 진입점에서 `configure_logging()` 호출. .env.example 에 `WARROOM_LOG_LEVEL` / `WARROOM_LOG_FORMAT` 가이드. (6) 단위 테스트 8건 (`common/tests/test_logging.py`). 248 → 256 pass (+8). |
