[project]
name = "warroom-orchestrator"
version = "0.1.0"
description = "Multi-Agent Orchestration - CrewAI pipeline"
requires-python = ">=3.13"
dependencies = [
    "crewai",
    "anthropic",
    "python-dotenv",
    "warroom-common",
]

[tool.uv.sources]
warroom-common = { workspace = true }
